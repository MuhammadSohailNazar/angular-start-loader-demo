language: node_js
node_js:
  - "12.14"
branches:
  only:
    - develop
    - master
cache:
  directories:
    - ./node_modules
install:
  - npm install
script:
  - npm test
  - npm run buildprod

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: master